{
  "name": "hyb-farm-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:wrangler": "node scripts/create-wrangler-config.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
